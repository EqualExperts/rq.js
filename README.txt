Allow a mongo user to 'name' and 'save' commonly-used queries, so they can be recalled and run at will with little effort

Download control-rq.js
In your mongo client, (at the shell), load control-rq.js using something like
